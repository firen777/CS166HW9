package decorator;

public abstract interface Evaluator
{
  /**Subclass to determine if Student match condition or not (?)
 * @param paramD Student
 * @return
 */
public abstract boolean evaluate(Student paramD);
}
